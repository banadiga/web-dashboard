How to use serve-static
=======================

Install server
--------------

To install `serve-static` run

```bash
npm install serve-static
```

Run serve-static
----------------

To start `serve-static` run
 
```bash
. ./server.sh
```
